# 测试框架主依赖 —— 用 `pip install -r requirements.txt` 安装。
# 后端被测系统的独立依赖见 apps/backend/requirements.txt（real 环境需另装）。

# HTTP & test
requests>=2.31.0,<3
urllib3>=2.0,<3
pytest>=8.0,<10
allure-pytest>=2.13.5,<3

# data driven
pandas>=2.2.0,<3
openpyxl>=3.1.2,<4

# local mock
flask>=3.0.0,<4

# json schema validation
jsonschema>=4.20,<5

# concurrent / xdist
pytest-xdist>=3.5,<4
