[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    smoke: 冒烟用例（核心主路径）
    slow: 较慢用例
filterwarnings =
    ignore::DeprecationWarning
