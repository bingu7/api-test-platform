[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    smoke: 冒烟用例（核心主路径）
    slow: 较慢用例
    real_env: 仅真实环境（非 dev）时运行
filterwarnings =
    ignore::DeprecationWarning
